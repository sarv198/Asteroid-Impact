# Python dependencies for Asteroid Impact Calculator
# No external dependencies required - uses only built-in Python modules
# The calculator uses only: math (built-in)

# If you want to add visualization capabilities, you could add:
# matplotlib>=3.5.0
# numpy>=1.21.0
# pandas>=1.3.0

# For web interface (optional):
# flask>=2.0.0
# flask-cors>=3.0.0
